name: Update README

on:
  push:
    branches:
      - origin/jihong  # Adjust based on your branch name

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Update README
        run: bash update_readme.sh